# Copilot Instructions

## Project Guidelines
- Preference: Do not write inline CSS or utility classes directly in .razor markup; always move styles into component-scoped .razor.css files and use semantic class names. Enforce this in agent prompts and CI. s